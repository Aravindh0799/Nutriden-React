import React from 'react'
import styles from '../styles/productInfo.module.css'

const ProductInfo = () => {
    return (
        <div className={`${styles.productInfoContainer} packetImageTimeline`}>product info</div>
    )
}

export default ProductInfo